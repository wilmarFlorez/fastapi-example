info:
  name: delete post by id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/posts/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
