info:
  name: create post
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/posts"
  body:
    type: json
    data: |
      {
        "title": "First post",
        "content": "First post prueba content",
        "rating": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
